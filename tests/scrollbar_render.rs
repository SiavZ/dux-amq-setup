// Placeholder integration-test crate for the scrollbar geometry tests
// scheduled in audit01 Phase 10 (`docs/plans/audits/audit01/10-scrollbar.md`).
//
// Intentionally empty: Cargo treats this file as its own crate but
// `cargo test` is happy with a test crate that defines no tests. Phase
// 10 will land the snapshot-style tests against the ratatui
// `Scrollbar` math used in `src/app/render.rs`.
